<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DixKontrol.MVVM.Vistas.ArchivoHistorico.MaterialHistoricoPage"
             Title="MaterialHistoricoPage">
    BackgroundColor="Transparent">

    <!-- FONDO DEGRADADO -->
    <ContentPage.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="White" Offset="0.0" />
            <GradientStop Color="Black" Offset="1.0" />
        </LinearGradientBrush>
    </ContentPage.Background>

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="25">

            <!-- TÍTULO -->
            <Label Text="Clips y material histórico"
                   FontSize="28"
                   FontAttributes="Bold"
                   TextColor="Black"
                   HorizontalTextAlignment="Center"
                   Margin="0,10" />

            <!-- DESCRIPCIÓN -->
            <Frame BackgroundColor="#10FFFFFF"
                   CornerRadius="12"
                   Padding="15">
                <Label Text="Una colección de fragmentos, grabaciones, flyers y documentos que forman parte de la memoria histórica de Nando Dixkontrol y de la escena electrónica barcelonesa."
                       FontSize="16"
                       TextColor="White"
                       LineHeight="1.35" />
            </Frame>

            <!-- CLIP 1 -->
            <Frame BackgroundColor="#20FFFFFF"
                   CornerRadius="12"
                   Padding="15">
                <VerticalStackLayout Spacing="8">

                    <Label Text="🎧 Fragmento de sesión — Psicódromo (1991)"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="White" />

                    <Label Text="Grabación recuperada de una de las noches más recordadas del Psicódromo. Energía pura, mezclas rápidas y el sonido característico de Nando."
                           FontSize="15"
                           TextColor="White"
                           LineHeight="1.35" />

                    <Button Text="Reproducir audio"
                            FontSize="14"
                            TextColor="White"
                            BackgroundColor="#40FFFFFF"
                            CornerRadius="10"
                            Clicked="ReproducirAudio1_Clicked"/>
                </VerticalStackLayout>
            </Frame>

            <!-- CLIP 2 -->
            <Frame BackgroundColor="#20FFFFFF"
                   CornerRadius="12"
                   Padding="15">
                <VerticalStackLayout Spacing="8">

                    <Label Text="🎥 Entrevista — Leyenda viva de la música electrónica (Vice)"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="White" />

                    <Label Text="Fragmento de una entrevista donde Nando reflexiona sobre la evolución de la música electrónica y el papel de Barcelona en la escena europea."
                           FontSize="15"
                           TextColor="White"
                           LineHeight="1.35" />

                    <Button Text="Ver clip"
                            FontSize="14"
                            TextColor="White"
                            BackgroundColor="#40FFFFFF"
                            CornerRadius="10"
                            Clicked="VerClip2_Clicked"/>
                </VerticalStackLayout>
            </Frame>

            <!-- CLIP 3 -->
            <Frame BackgroundColor="#20FFFFFF"
                   CornerRadius="12"
                   Padding="15">
                <VerticalStackLayout Spacing="8">

                    <Label Text="📄 Flyer original — Psicódromo (1990)"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="White" />

                    <Label Text="Uno de los flyers más icónicos de la época, distribuido en tiendas de discos y puntos de encuentro de la escena underground."
                           FontSize="15"
                           TextColor="White"
                           LineHeight="1.35" />

                    <Button Text="Ver imagen"
                            FontSize="14"
                            TextColor="White"
                            BackgroundColor="#40FFFFFF"
                            CornerRadius="10"
                            Clicked="VerFlyer3_Clicked"/>
                </VerticalStackLayout>
            </Frame>

            <!-- CLIP 4 -->
            <Frame BackgroundColor="#20FFFFFF"
                   CornerRadius="12"
                   Padding="15">
                <VerticalStackLayout Spacing="8">

                    <Label Text="🎙️ Testimonio — DJ invitado (1992)"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="White" />

                    <Label Text="Declaraciones de un DJ que compartió cabina con Nando, recordando su técnica, su carisma y su influencia en la escena."
                           FontSize="15"
                           TextColor="White"
                           LineHeight="1.35" />

                    <Button Text="Escuchar testimonio"
                            FontSize="14"
                            TextColor="White"
                            BackgroundColor="#40FFFFFF"
                            CornerRadius="10"
                            Clicked="Testimonio4_Clicked"/>
                </VerticalStackLayout>
            </Frame>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
